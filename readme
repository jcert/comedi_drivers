	must change the USB_DEVICE(0x3923, 0x7348) to have the vendor ID and product ID to our 
	device one, should we make the same as the layman's Arduino or create a new one? Will 
	this be a problem if a non-configured Arduino is plugged?
